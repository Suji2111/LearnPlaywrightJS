console.log(API_END_APP_VWO_COM);
console.log("dasda")
if (true) {

}

var API_END_APP_VWO_COM = "https://app.vwo.com/login/api";
// if we give let or const instead of var, it will throw a reference error because of TDZ (Temporal Dead Zone) which means that the variable is in a dead zone from the start of the block until the declaration is processed. During this time, the variable cannot be accessed and will throw a reference error if we try to access it. With var, the variable is hoisted and initialized with undefined, so it does not throw an error when accessed before its declaration.
